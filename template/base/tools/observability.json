{
  "//": "The observability containment register (gate: observability, tools/check-observability.mjs). vendorSpecifiers is the telemetry-SDK detector — it may be EXTENDED for a vendor the shipped floor has never heard of, never narrowed below REQUIRED_VENDOR_FLOOR in tools/lib/observability.mjs (the gate reds a missing floor entry). sinks[] is the reviewed register of the ONLY files allowed to import a vendor telemetry SDK: each entry is { file, vendors, redaction, reason } — the file must import a listed vendor, reference its redaction symbol in code, and carry a reason of 40+ characters, because registering a sink is licensing an off-device egress path for operational data (see packages/platform/observability/src/index.ts, 'NO VENDOR SDK, on purpose'). The shipped state declares zero sinks: the seam's LogSink is where a vendor transport attaches, behind redactFields, via the crash-reporting/observability module patches — land the sinks[] row in the SAME diff as the patch. Seeded (yours after init) and planted when absent; git-clean-enforced by check-gate-integrity.mjs.",
  "vendorSpecifiers": [
    "@amplitude/",
    "@bugsnag/",
    "@datadog/",
    "@fullstory/",
    "@grafana/faro-react-native",
    "@grafana/faro-web-sdk",
    "@highlight-run/",
    "@honeycombio/",
    "@microsoft/applicationinsights-web",
    "@newrelic/",
    "@opentelemetry/",
    "@react-native-firebase/analytics",
    "@react-native-firebase/crashlytics",
    "@react-native-firebase/perf",
    "@segment/",
    "@sentry/",
    "@vercel/otel",
    "analytics-node",
    "applicationinsights",
    "dd-trace",
    "expo-insights",
    "firebase/analytics",
    "logrocket",
    "mixpanel",
    "mixpanel-browser",
    "mixpanel-react-native",
    "newrelic",
    "posthog-js",
    "posthog-node",
    "posthog-react-native"
  ],
  "redactionSymbols": ["redactFields"],
  "sinks": []
}
